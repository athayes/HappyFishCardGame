stages:
  - build
  - test

backend:
  stage: build
  image: alpine:3.15
  script:
    - apk add ruby git
    - apk add py3-pip
    - gem install dpl
    - cd backend
    - dpl --provider=heroku --app=happyfishcardgame --api-key=${HEROKU_KEY}
